namespace: keycloak

keycloak:
  name: keycloak

realm:
  name: spiffe1

hook:
  image:
    repository: quay.io/cloud-architecture-workshop/wait-for-statefulset-job
    pullPolicy: IfNotPresent
    tag: "1c68180"
  serviceAccount:
    create: true
    name: ""
  podSecurityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL
  maxTimeToWait: 120000

clients:
  complaintsClient:
    id:
    secret:
  mcpClient:
    id:
    secret:
  mcpServer:
    id:
    secret:
    scope:
  mcpService:
    id:
    secret:
  agentSpiffe:
    id:
    secret:
    spiffeId:

spiffe:
  enabled: true
  trustDomain: globex.com
  bundleEndpoint:
  providerId: spiffe

groups: []

roles: []

argocd:
  enabled: true
  syncwave: "-2"
