{{- if .Values.globex.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.globex.applicationName }}
  namespace: {{ .Values.globex.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.globex.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.globex.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.globex.source.repoUrl }}
    targetRevision: {{ .Values.globex.source.targetRevision }}
    path: {{ .Values.globex.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.globex.source.helm.values | nindent 8 }}
{{- end }}
