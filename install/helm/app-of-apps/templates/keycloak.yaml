{{- if .Values.keycloak.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.keycloak.applicationName }}
  namespace: {{ .Values.keycloak.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.keycloak.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.keycloak.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.keycloak.source.repoUrl }}
    targetRevision: {{ .Values.keycloak.source.targetRevision }}
    path: {{ .Values.keycloak.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.keycloak.source.helm.values | nindent 8 }}
{{- end }}
