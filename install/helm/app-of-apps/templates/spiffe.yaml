{{- if .Values.spiffe.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.spiffe.applicationName }}
  namespace: {{ .Values.spiffe.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.spiffe.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.spiffe.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.spiffe.source.repoUrl }}
    targetRevision: {{ .Values.spiffe.source.targetRevision }}
    path: {{ .Values.spiffe.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.spiffe.source.helm.values | nindent 8 }}
{{- end }}
