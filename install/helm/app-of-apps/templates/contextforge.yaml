{{- if .Values.contextforge.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.contextforge.applicationName }}
  namespace: {{ .Values.contextforge.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.contextforge.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.contextforge.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.contextforge.source.repoUrl }}
    targetRevision: {{ .Values.contextforge.source.targetRevision }}
    path: {{ .Values.contextforge.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.contextforge.source.helm.values | nindent 8 }}
{{- end }}